name: CI/CD for Nuxt.js

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1

      - name: Login to Docker Hub
        uses: docker/login-action@v1
        with:
          username: mahdiharzallah
          password: dckr_pat_MxgDLA5VOIspKUXIzdBugO8i_3Y

      - name: Build and push Docker image
        run: |
          docker build -t mahdiharzallah/nest-git:latest .
          docker push mahdiharzallah/nest-git:latest
